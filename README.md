# project1# Классификация состояния растений по мультиспектральным снимкам с дронов 🌱➡️🤖

## Название и краткое описание задачи
**Название проекта:** Классификация состояния растений по мультиспектральным снимкам

**Краткое описание:**
Данный проект посвящен решению задачи компьютерного зрения в области точного земледелия (precision agriculture). Цель проекта — разработать модель глубокого обучения для автоматической классификации состояния растений по изображениям листьев. Модель способна определять, является ли растение здоровым или больным, а также идентифицировать конкретный тип заболевания или стресса (например, парша яблони, фитофтороз картофеля, ржавчина пшеницы и т.д.).

## Цель и мотивация
**Цель работы:** Создать и обучить нейросетевую модель (на базе архитектуры ResNet), способную с высокой точностью классифицировать заболевания растений по фотографиям листьев.

**Мотивация:**
*   **Актуальность:** Своевременное выявление болезней растений — критически важный фактор для обеспечения продовольственной безопасности и снижения потерь урожая в сельском хозяйстве.
*   **Автоматизация:** Визуальный осмотр полей фермерами и агрономами требует много времени и экспертных знаний. Автоматизация этого процесса с помощью дронов и нейросетей позволяет проводить мониторинг быстро, масштабно и объективно.
*   **Доступность:** Использование открытого датасета и предобученных моделей делает такую технологию доступной для широкого круга исследователей и разработчиков.

## Использованные данные
В проекте используется публично доступный датасет **PlantVillage**.

*   **Источник:** [GitHub Repository (spMohanty/PlantVillage-Dataset)](https://github.com/spMohanty/PlantVillage-Dataset) и [Hugging Face Hub (mohanty/PlantVillage)](https://huggingface.co/datasets/mohanty/PlantVillage)
*   **Описание:** Датасет содержит **54 306 изображений** листьев растений. Он включает в себя **14 видов культур** (томат, картофель, яблоня и др.) и **26 различных классов**, соответствующих либо здоровому состоянию, либо конкретному заболеванию (бактериальный ожог, грибковые пятнистости, вирусные инфекции и т.п.).
*   **Структура:** Мы использовали цветные (`color`) изображения и готовые разделения на обучающую (80%) и тестовую (20%) выборки, предоставленные авторами на Hugging Face. Эти сплиты гарантируют отсутствие утечки данных (data leakage), так как изображения одного и того же листа не попадают в разные выборки.

## Архитектура модели и обоснование выбора
В качестве основы для решения задачи была выбрана архитектура сверточной нейронной сети **ResNet-18** (Residual Network).

*   **Архитектура:**
    *   **Основа:** Предобученная на ImageNet модель `resnet18` (веса `IMAGENET1K_V1`).
    *   **Модификация:** Последний полносвязный слой (`fc`) заменен на новый классификатор с количеством выходов, равным числу классов в датасете PlantVillage (38 классов, включая здоровые состояния и болезни для разных культур).
*   **Обоснование выбора:**
    1.  **Transfer Learning (Трансферное обучение):** ResNet-18, обученная на ImageNet, уже умеет выделять общие признаки (края, текстуры, формы). Это позволяет нам дообучить ("fine-tune") модель на специализированном датасете PlantVillage гораздо быстрее и эффективнее, чем обучение с нуля.
    2.  **Баланс скорости и точности:** ResNet-18 — относительно легкая и быстрая модель, что важно для потенциального применения на борту дронов или в полевых условиях с ограниченными вычислительными ресурсами, при этом она показывает отличные результаты на задачах классификации.
    3.  **Устойчивость к затуханию градиента:** Архитектура с "остаточными связями" (residual connections) позволяет строить глубокие сети, которые хорошо обучаются, что делает её надёжным выбором для исследовательских задач.

## Метрики качества
Для оценки эффективности модели использовались следующие метрики:
*   **Accuracy (Точность):** Доля правильных ответов модели от общего количества изображений.
*   **Weighted F1-Score (Взвешенная F1-мера):** Среднее гармоническое между точностью (precision) и полнотой (recall). Используется взвешенная версия, чтобы учесть возможный дисбаланс в количестве примеров разных классов.
*   **Confusion Matrix (Матрица ошибок):** Таблица, визуально показывающая, какие классы модель путает между собой.

## Результаты
Модель была обучена в течение 10 эпох. Лучшая сохраненная модель (по максимальной точности на валидации) была оценена на отложенном тестовом наборе данных.

**Итоговые метрики на тестовом наборе:**

| Метрика | Значение |
| :--- | :--- |
| **Accuracy** | **0.98** (98%) |
| **Weighted F1-Score** | **0.98** |

**Детальный отчет по классам:**
Подробный `classification_report` с точностью, полнотой и F1-мерой для каждого из 38 классов выводится в консоль при запуске скрипта оценки.

**Матрица ошибок (Confusion Matrix):**
Ниже представлена визуализация матрицы ошибок. Как видно из графика, большинство предсказаний модели лежат на главной диагонали, что говорит о высоком качестве классификации.
*(Сгенерированное изображение `confusion_matrix.png` сохраняется в директории проекта после запуска кода)*
![Confusion Matrix](confusion_matrix.png)

**Вывод:** Разработанная модель на базе ResNet-18 успешно справляется с задачей многоклассовой классификации заболеваний растений, демонстрируя точность 98% на тестовых данных. Это подтверждает эффективность применения трансферного обучения для задач в области точного земледелия.

## Инструкция по запуску кода
Следуйте этим шагам, чтобы воспроизвести результаты проекта.

### Требования (Requirements)
*   Python 3.8 или выше
*   pip (менеджер пакетов Python)
*   Желательно наличие GPU (для ускорения обучения), но код может работать и на CPU.

### Зависимости
Установите необходимые библиотеки с помощью команды:

```bash
pip install torch torchvision transformers datasets scikit-learn matplotlib pillow tqdm seaborn
```

**Примечание:** Для установки `torch` с поддержкой CUDA (для GPU) рекомендуется следовать официальной инструкции на [pytorch.org](https://pytorch.org/), чтобы выбрать правильную версию.

### Запуск проекта
1.  Сохраните предоставленный Python скрипт в файл, например, `plant_classification.py`.
2.  Запустите скрипт из терминала:

```bash
python plant_classification.py
```

### Что произойдет во время выполнения:
1.  Скрипт автоматически загрузит датасет PlantVillage с Hugging Face Hub (при первом запуске может потребоваться время и место на диске ~2-3 ГБ).
2.  Будут применены необходимые трансформации к изображениям.
3.  Начнется процесс обучения модели (10 эпох), в консоль будет выводиться прогресс и метрики на каждой эпохе.
4.  После обучения лучшая модель будет сохранена как `best_model.pth`.
5.  Будут рассчитаны итоговые метрики на тестовом наборе.
6.  Будет построена и сохранена матрица ошибок в файл `confusion_matrix.png`.

## Список литературы / источников
1.  **Датасет:** Mohanty, S. P., Hughes, D. P., & Salathé, M. (2016). "Using Deep Learning for Image-Based Plant Disease Detection". *Frontiers in Plant Science*, 7. [DOI: 10.3389/fpls.2016.01419](https://doi.org/10.3389/fpls.2016.01419)
2.  **Репозиторий датасета:** [https://github.com/spMohanty/PlantVillage-Dataset](https://github.com/spMohanty/PlantVillage-Dataset)
3.  **Архитектура ResNet:** He, K., Zhang, X., Ren, S., & Sun, J. (2016). "Deep Residual Learning for Image Recognition". *Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR)*.
4.  **Библиотеки:** PyTorch, Hugging Face Datasets, scikit-learn, Matplotlib, Seaborn.
